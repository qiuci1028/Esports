# Riot电竞大数据分析平台 Web 网页排版设计文档
> 适用场景：毕设可视化页面、PC端1920×1080标准大屏，整体风格：**科技电竞风 + 深色大数据可视化**

## 一、全站统一视觉基础规范
### 1. 配色体系
| 类型      | 色值             | 使用场景                               |
| --------- | ---------------- | -------------------------------------- |
| 主色调    | `#1677FF` 科技蓝 | 页面标题、功能按钮、高亮图表、选中状态 |
| 页面背景  | `#0F172A` 深黑蓝 | 全站页面底层背景                       |
| 卡片背景  | `#1E293B` 深灰蓝 | 图表卡片、信息面板、弹窗容器           |
| 分割/边框 | `#334155` 浅灰蓝 | 模块分割线、卡片边框、表格边线         |
| 一级文字  | `#F8FAFC` 纯白   | 大标题、核心数据、正文内容             |
| 辅助文字  | `#94A3B8` 浅灰   | 标签、备注、说明文本                   |
| 成功/胜利 | `#10B981` 翠绿   | 胜率数据、对局胜利标识                 |
| 失败/禁用 | `#EF4444` 赤红   | 对局失败、高禁用率、告警提示           |
| 热度/警示 | `#F59E0B` 暖橙   | 热门英雄、实时热度标签                 |

### 2. 字体规范
- 通用字体：微软雅黑 / 思源黑体（无衬线字体，大屏展示清晰）
- 字号层级：
  - 页面大标题：`28px` 加粗
  - 模块/卡片标题：`20px` 加粗
  - 核心数据数值：`24~32px` 加粗
  - 正文、表格内容：`14px` 常规
  - 辅助备注小字：`12px` 常规
- 行高：标题 `1.2`，正文 `1.5`

### 3. 间距与组件通用样式
- 页面全局外边距：左右 `24px`，顶部 `16px`
- 模块间距：区块/卡片之间统一 `20px`
- 卡片内边距：上下 `16px`、左右 `20px`
- 圆角：卡片、按钮统一 `8px`
- 卡片阴影：`0 2px 12px rgba(0,0,0,0.3)`

### 4. 全局顶部导航栏（所有页面通用）
- 尺寸：通栏全屏，高度 `60px`，固定悬浮置顶
- 布局分区（从左至右）：
  1. 左侧：平台Logo + 系统名称「Riot电竞大数据分析平台」
  2. 中间：游戏切换标签（英雄联盟 / 无畏契约 / 云顶之弈）
  3. 右侧：版本选择下拉框 + 刷新按钮 + 系统时间/运行状态
- 对齐规则：所有内容**垂直居中**，水平按需分布。

---

## 二、核心页面排版设计
### 页面一：数据可视化大屏（首页·核心页面）
整体版式：**顶部导航 → 核心指标区 → 主体图表区 → 底部公告区**，采用**左右分区+上下嵌套**混合布局。

#### 1. 顶部导航区
沿用全局导航规范，通栏置顶。

#### 2. 核心指标区（第二行）
- 布局：**四列等宽卡片横向并排**，四卡等高、间距 `20px`
- 单卡片内部排版（从上至下）：
  1. 12px浅灰小字：指标名称
  2. 24~32px加粗主色大字：统计数值
  3. 12px绿/红小字：数据环比涨跌
- 展示指标：总比赛量、活跃玩家、全区平均段位、实时在线人数

#### 3. 主体图表区（页面核心，占页面70%高度）
整体分**左侧主区域（65%宽度）**、**右侧副区域（35%宽度）**

##### （1）左侧主区域
内部拆分为「上一大块 + 下两小块」
1. 左上大卡片（左区60%高度）
   - 顶部：20px加粗模块标题「版本英雄BP胜率分析」
   - 主体：横向柱状图，图表填满卡片剩余区域
2. 左下双列卡片（左区40%高度，左右等分）
   - 左卡片：标题「24小时英雄热度趋势」+ 折线图
   - 右卡片：标题「全区段位人数分布」+ 饼图
   - 两卡片等高等宽，间距 `20px`

##### （2）右侧副区域
内部拆分为「上两小卡片 + 下一大卡片」
1. 右上第一卡片：版本T0热门英雄TOP5
   - 单行布局：圆形头像 + 英雄名称 + 胜率数值，竖向列表排列
2. 右上第二卡片：实时上分榜单
   - 单行布局：排名序号 + 玩家昵称 + 段位积分，竖向列表排列
3. 右下大卡片：实时滚动赛事
   - 内容：比赛信息流，横向走马灯滚动展示「队伍VS队伍+对局结果」

#### 4. 底部公告区（最下方通栏，高度80px）
- 布局：单张通栏卡片
- 排版：左侧版本公告、右侧数据更新时间+数据源说明
- 文字样式：统一使用12px浅灰辅助文字

#### 大屏结构简图

┌─────────────────────────────────────────────────────┐

│ 导航栏 Logo  游戏标签  版本  刷新  状态 (通栏)        │

├─────┬─────┬─────┬─────┬─────────────────────────────┤

│指标卡 1│指标卡 2│指标卡 3│指标卡 4│  顶部数据行（4 等分）   │

├─────┴─────┴─────┴─────┼───────────┬─────────────────┤

│                       │ 右上 TOP5 榜单 │               │

│                       ├───────────┤  右侧区域       │

│    左侧大图表区        │ 右上实时榜单 │  (35% 宽度)    │

│  (65% 宽度)            ├───────────┤                 │

│                       │ 右下实时赛事 │               │

├───────────────────────┴───────────┴─────────────────┤

│ 底部通栏：版本说明 + 数据更新时间                     │

└─────────────────────────────────────────────────────┘

---

### 页面二：玩家档案查询页（功能查询页）
整体版式：**顶部导航 → 搜索区 → 玩家信息区 → 图表分析区 → 比赛记录表**

#### 1. 顶部导航区
沿用全局导航规范。

#### 2. 搜索筛选区（独立区块）
- 布局：整行水平居中排布
- 元素从左至右：游戏下拉框 → 大区下拉框 → 召唤师名称输入框 → 查询按钮
- 尺寸规范：输入框宽度 `280px`，查询按钮宽度 `100px`，组件水平间距 `16px`
- 对齐：所有组件垂直居中

#### 3. 玩家基础信息卡片（通栏）
- 布局：**左右分栏**
  1. 左侧：80×80px圆形玩家头像，垂直居中
  2. 右侧文字区（从上至下竖向排版）
     - 一级标题：召唤师名称（28px加粗主色）
     - 第二行：PUUID、游戏、大区、角色等级（辅助文字横向排列）
     - 第三行：当前段位、积分、总胜负场（彩色标注关键数据）
     - 第四行：最近上线时间、综合胜率（辅助说明）
- 整体：左右板块垂直居中，留白充足。

#### 4. 数据分析图表区
- 布局：**2行2列标准网格**，四张卡片等高等宽，行列间距 `20px`
- 卡片内容：
  1. 左上：英雄熟练度TOP5（雷达图/横向条形图）
  2. 右上：近3赛季段位走势（折线图）
  3. 左下：近20场KDA统计（柱状图）
  4. 右下：玩家活跃时段分布（饼图）
- 通用规则：每张卡片顶部为模块标题，下方填充完整图表。

#### 5. 比赛记录列表区（通栏表格）
- 布局：标准数据表格，通栏展示
- 表头：比赛ID、使用英雄、对局结果、KDA、对局时长、经济/伤害
- 样式规则：奇偶行底色区分；胜利行文字标绿，失败行文字标红；单元格内容居中；英雄搭配24×24px小图标展示。

#### 6. 附加模块（可选：对局胜率预测）
独立小卡片，置于表格上方/下方、页面居中：
- 顶部：模块标题
- 中部：进度条可视化胜率占比
- 底部：文字说明（预测概率 + 核心影响特征）

#### 玩家查询页结构简图

┌─────────────────────────────────────────────────────┐

│ 导航栏 (通栏)                                       │

├─────────────────────────────────────────────────────┤

│ 【搜索区】下拉框 + 输入框 + 查询按钮 (居中)           │

├─────────────────────────────────────────────────────┤

│ 【玩家信息卡】头像 ｜ 名称 / 段位 / 数据 (左右布局)       │

├───────────┬───────────┬─────────────────────────────┤

│ 图表卡 1   │ 图表卡 2   │  2 行 2 列 图表网格区           │

├───────────┼───────────┤                             │

│ 图表卡 3   │ 图表卡 4   │                             │

├───────────┴───────────┴─────────────────────────────┤

│ 【比赛记录】数据表格 (通栏)                          │

└─────────────────────────────────────────────────────┘

---

### 页面三：英雄/特工数据分析页
整体版式：**顶部导航 → 筛选栏 → 汇总指标区 → 多维度图表区 → 详情列表区**

#### 1. 顶部导航区
沿用全局导航规范。

#### 2. 筛选条件区（顶部通栏）
- 布局：单行水平排布，组件居中对齐
- 元素从左至右：游戏切换 → 游戏模式 → 版本选择 → 英雄位置筛选 → 刷新按钮

#### 3. 汇总指标区
- 布局：**四列等宽卡片横向并排**，与大屏指标卡样式统一
- 展示指标：全场平均出场率、综合胜率、平均禁用率、英雄热门指数

#### 4. 主体图表区
- 布局：**左、中、右三栏并列**，宽度比例 `3:2:2`
  1. 左栏（最大区块）：全英雄BP胜率排行（纵向柱状图）
  2. 中栏：英雄属性分布（雷达图，图形居中展示）
  3. 右栏：英雄每日热度变化（折线图）

#### 5. 详情列表区（底部通栏）
- 布局：标准数据表格，支持表头点击排序
- 列表字段：英雄名称、定位位置、出场率、胜率、禁用率、平均KDA
- 样式：图标+文字组合排版，单元格居中对齐。

---

### 页面四：比赛详情页（单场对局分析）
整体版式：**顶部导航 → 对局基础信息 → 双方队伍对比区 → 对局时序区 → 数据明细表格**

#### 1. 顶部导航区
沿用全局导航规范。

#### 2. 对局基础信息（通栏顶部）
- 布局：整行文字居中排版
- 展示内容：比赛ID、所属游戏、对战地图、对局时长、游戏版本、结束时间
- 样式：区分标题字号，主次文本层级分明。

#### 3. 双方队伍对比区（核心区域）
- 布局：**左右对称双栏**，中间竖线分割（蓝方队伍 / 红方队伍）
- 单栏内部竖向排版：
  1. 分区标题：蓝方 / 红方
  2. 队员列表：单行布局（头像 + 英雄图标 + 昵称 + KDA）
  3. 团队汇总数据：总击杀/死亡/助攻、总经济、总伤害（横向并列小字）

#### 4. 对局时序分析区（通栏大卡片）
- 内容：比赛时间线、关键事件（一血、野区资源、团战）时序图
- 排版：横轴为时间、纵轴为对局事件，标签紧贴图形，布局紧凑。

#### 5. 对局数据明细（底部通栏表格）
标准数据表格，分维度展示经济、视野、技能使用等对局明细，单元格居中对齐。

---

## 三、通用组件排版规范
### 1. 图表卡片（全站复用）
固定内部结构（从上至下）：
1. 卡片标题（居左，20px 加粗）
2. 留白 `12px`
3. 图表区域（填满卡片剩余高度）
> 要求：全站图表卡片尺寸比例统一，无大面积留白。

### 2. 榜单/列表行项目
单行固定结构（从左至右）：
`排名序号 → 头像/图标 → 名称 → 数据数值 → 状态标签`
- 所有列表行高度统一，内容垂直居中。

### 3. 表单控件（输入框、下拉框、按钮）
- 主按钮：主色背景、白色文字、8px圆角，hover状态浅高亮
- 输入框/下拉框：深色边框、浅底色，文字左对齐
- 整行表单组件：全部垂直居中对齐。

---

## 四、落地与论文使用建议
1. **原型制作**：基于本文档尺寸、配色、分区规则，使用 Figma、墨刀、PPT 绘制页面效果图，可直接作为毕设配图。
2. **答辩演示**：优先展示**数据大屏**（视觉效果最佳），其次玩家查询页，重点讲解分区布局、数据层级、可视化排版。
3. **论文配图**：每类页面截取「整体布局图 + 局部细节图」，并标注区块名称（导航区、指标区、图表区等）。
4. **设备适配**：文档排版基于 `1920×1080` PC大屏，毕设以PC端为核心，无需复杂移动端适配。



---

### 页面三：英雄/特工数据分析页
整体版式：**顶部导航 → 筛选栏 → 汇总指标区 → 多维度图表区 → 详情列表区**

#### 1. 顶部导航区
沿用全局导航规范。

#### 2. 筛选条件区（顶部通栏）
- 布局：单行水平排布，组件居中对齐
- 元素从左至右：游戏切换 → 游戏模式 → 版本选择 → 英雄位置筛选 → 刷新按钮

#### 3. 汇总指标区
- 布局：**四列等宽卡片横向并排**，与大屏指标卡样式统一
- 展示指标：全场平均出场率、综合胜率、平均禁用率、英雄热门指数

#### 4. 主体图表区
- 布局：**左、中、右三栏并列**，宽度比例 `3:2:2`
  1. 左栏（最大区块）：全英雄BP胜率排行（纵向柱状图）
  2. 中栏：英雄属性分布（雷达图，图形居中展示）
  3. 右栏：英雄每日热度变化（折线图）

#### 5. 详情列表区（底部通栏）
- 布局：标准数据表格，支持表头点击排序
- 列表字段：英雄名称、定位位置、出场率、胜率、禁用率、平均KDA
- 样式：图标+文字组合排版，单元格居中对齐。

---

### 页面四：比赛详情页（单场对局分析）
整体版式：**顶部导航 → 对局基础信息 → 双方队伍对比区 → 对局时序区 → 数据明细表格**

#### 1. 顶部导航区
沿用全局导航规范。

#### 2. 对局基础信息（通栏顶部）
- 布局：整行文字居中排版
- 展示内容：比赛ID、所属游戏、对战地图、对局时长、游戏版本、结束时间
- 样式：区分标题字号，主次文本层级分明。

#### 3. 双方队伍对比区（核心区域）
- 布局：**左右对称双栏**，中间竖线分割（蓝方队伍 / 红方队伍）
- 单栏内部竖向排版：
  1. 分区标题：蓝方 / 红方
  2. 队员列表：单行布局（头像 + 英雄图标 + 昵称 + KDA）
  3. 团队汇总数据：总击杀/死亡/助攻、总经济、总伤害（横向并列小字）

#### 4. 对局时序分析区（通栏大卡片）
- 内容：比赛时间线、关键事件（一血、野区资源、团战）时序图
- 排版：横轴为时间、纵轴为对局事件，标签紧贴图形，布局紧凑。

#### 5. 对局数据明细（底部通栏表格）
标准数据表格，分维度展示经济、视野、技能使用等对局明细，单元格居中对齐。

---

## 三、通用组件排版规范
### 1. 图表卡片（全站复用）
固定内部结构（从上至下）：
1. 卡片标题（居左，20px 加粗）
2. 留白 `12px`
3. 图表区域（填满卡片剩余高度）
> 要求：全站图表卡片尺寸比例统一，无大面积留白。

### 2. 榜单/列表行项目
单行固定结构（从左至右）：
`排名序号 → 头像/图标 → 名称 → 数据数值 → 状态标签`
- 所有列表行高度统一，内容垂直居中。

### 3. 表单控件（输入框、下拉框、按钮）
- 主按钮：主色背景、白色文字、8px圆角，hover状态浅高亮
- 输入框/下拉框：深色边框、浅底色，文字左对齐
- 整行表单组件：全部垂直居中对齐。

---

## 四、落地与论文使用建议
1. **原型制作**：基于本文档尺寸、配色、分区规则，使用 Figma、墨刀、PPT 绘制页面效果图，可直接作为毕设配图。
2. **答辩演示**：优先展示**数据大屏**（视觉效果最佳），其次玩家查询页，重点讲解分区布局、数据层级、可视化排版。
3. **论文配图**：每类页面截取「整体布局图 + 局部细节图」，并标注区块名称（导航区、指标区、图表区等）。
4. **设备适配**：文档排版基于 `1920×1080` PC大屏，毕设以PC端为核心，无需复杂移动端适配。